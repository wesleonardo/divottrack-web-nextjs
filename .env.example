# Environment example for Google OAuth (Google Cloud Identity Provider)
# Configure these values in your environment or .env.local (do NOT commit secret values)

# Public site base URL (used for building redirect URIs)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Google OAuth client credentials
GOOGLE_OAUTH_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:3000/api/auth/callback

# (Optional) Service account JSON for server-side operations
# Option A: Put the entire JSON string in env (not recommended to commit)
# GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...","client_email":"..."}'
# Option B (recommended for local dev): Point to a downloaded JSON file (add filename to .gitignore)
# GOOGLE_APPLICATION_CREDENTIALS=.downloaded-filename.json
